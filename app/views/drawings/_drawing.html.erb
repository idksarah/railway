<div id="<%= dom_id drawing %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <strong class="block font-medium mb-1">Artist:</strong>
    <%= drawing.artist %>
    <%= image_tag (drawing.image) if drawing.image.attached?%>
  </div>
</div>
